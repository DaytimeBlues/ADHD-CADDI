#!/bin/sh
# Pre-commit hook for ADHD-CADDI
# This hook runs lint-staged on staged files

# Use --no-stash to avoid Windows git stash issues
# The stash backup feature can fail on Windows with "Needed a single revision"
npx lint-staged --no-stash
